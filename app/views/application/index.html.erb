<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
      <%=raw content_tag :div, msg, id:"flash_#{name}" %>
    </div>
  <% end %>
<% end %>

<h2 class="title">Welcome to the new ASSU Petitioning Interface!</h2>
Feel free to explore the public pages, login and create/sign petitions.
<br><br>
Things that still need to be done:
<ul>
  <li>Bulk import of grad/undergrad status</li>
  <li>Do we want to restict who can sign what type of petition?</li>
  <li>Should there be different types of petitions?</li>
  <li>Prevent people from editing after N signatures</li>
</ul>
<br>
Developed by Tim Vrakas, send all feeback to timv@assu.stanford.edu

</ul>